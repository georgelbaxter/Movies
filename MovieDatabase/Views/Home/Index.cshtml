
<body ng-app="MovieApp">
    <div ng-controller="MovieController">
        <br />
        <div>
            <ng-form>
                <div class="form-group row">
                    Title: <input type="text" ng-model="movieToCreate.Name" /> &nbsp; 
                    Year: <input type="number" ng-model="movieToCreate.Year" />
                </div>
                <div class="form-group row">
                    <button id="btnCreateMovie" class="btn btn-default" ng-click="createMovie()">Add Movie</button>
                </div>
            </ng-form>
        </div>
        <table class="table">
            <tr>
                <th>Title</th>
                <th>Year</th>
                <th>Genre</th>
                <th colspan="4">Actors</th>
            </tr>
            <tr ng-repeat="m in movies.data">
                <td>{{m.Name}}</td>
                <td>{{m.Year}}</td>
                <td>{{m.Genre}}</td>
                <td ng-repeat="a in m.Actors">{{a}}</td>
            </tr>
        </table>
    </div>
</body>